// If we dont use return keyword and try to store value in variable at the time of execuation it show undefined.

function data(){
    let username = 'mama'
    console.log (`${username} is Logged in just`);
}

data()